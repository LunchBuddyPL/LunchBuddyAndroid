<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dark_grass_green</item>
        <item name="colorPrimaryDark">@color/dark</item>
        <item name="colorAccent">@color/mango</item>
        <item name="android:navigationBarColor">?android:attr/colorPrimary</item>
        <item name="android:statusBarColor">?android:attr/colorPrimary</item>
        <item name="android:windowBackground">@drawable/background</item>
    </style>
    
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/background_start_layers</item>
    </style>

</resources>
